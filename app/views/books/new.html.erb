<h2>New book</h2>
<%= form_with model: @book do |f| %>
  <div>Title</div>
  <%= f.text_field :title %>

  <div>Opinion</div>
  <%= f.text_area :body %>

  <div><%= f.submit 'Create Book', class:"btn btn-success mt-2" %></div>
<% end %>